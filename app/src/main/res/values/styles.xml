<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="fontFamily">@font/montserrat</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="SplashScreenTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="StocksFragment.TabLayout" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabMode">fixed</item>
        <item name="tabGravity">start</item>

        <!--        Disable tab indicator and ripple-->
        <item name="tabIndicatorColor">@android:color/transparent</item>
        <item name="tabRippleColor">@android:color/transparent</item>

        <item name="tabTextAppearance">@style/StocksFragment.TabLayout.Tab.Default</item>

        <item name="tabTextColor">@color/colorTextInactive</item>
        <item name="tabSelectedTextColor">@color/colorTextActive</item>
    </style>

    <style name="StocksFragment.TabLayout.Tab.Default" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:fontWeight">700</item>
        <item name="android:textSize">28sp</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.CornerSize10Percent" parent="">
        <item name="cornerSize">10%</item>
    </style>

</resources>